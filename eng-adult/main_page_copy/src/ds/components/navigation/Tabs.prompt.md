Switches between sibling views: lesson formats on marketing pages, dashboard sections in the app.

```jsx
<Tabs items={['Індивідуально','У групі','Speaking Club']} value={fmt} onChange={setFmt}/>
```

`pill` sits on a `--base-100` track with a white raised thumb. `underline` marks the active tab with a 3px orange rule.
