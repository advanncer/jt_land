Multi-select control — consent lines, lesson-goal lists, notification settings.

```jsx
<Checkbox checked={ok} onChange={setOk} label="Погоджуюсь з умовами" description="Договір-оферта · Конфіденційність"/>
```

The only square-cornered control in the system (4px). Use `Switch` instead when the change applies immediately.
