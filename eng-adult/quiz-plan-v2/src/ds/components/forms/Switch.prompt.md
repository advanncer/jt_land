Toggle for settings that apply instantly — reminders, camera, sound.

```jsx
<Switch checked={remind} onChange={setRemind} label="Нагадування про урок"/>
```

Off state is `--base-400`, on is brand orange. The knob overshoots slightly on travel; that bounce is the brand's signature motion.
