Exclusive choice — lesson length, format (individual / group), payment plan.

```jsx
<Radio checked={len===50} onChange={()=>setLen(50)} label="50 хвилин" description="студент + викладач · онлайн"/>
```

22px circle with a 2px ring; the ring turns orange and fills with a 10px dot when selected.
