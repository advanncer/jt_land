Explains an icon-only control or an abbreviated label.

```jsx
<Tooltip content="Вимкнути мікрофон"><IconButton label="mic">…</IconButton></Tooltip>
```

Always `--base-900` on white text at 12px SemiBold; 8px radius. Appears on hover with no delay.
